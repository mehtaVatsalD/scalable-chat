# Test-specific Redis configuration
spring.data.redis.host=localhost
spring.data.redis.port=6379

# Use a clean prefix for tests to avoid collisions with local dev data
# (If we implement dynamic prefixing later, otherwise just standard config)
