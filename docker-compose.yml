version: '3.8'

services:
  postgres:
    image: postgres:15
    container_name: scalable-chat-db
    environment:
      POSTGRES_DB: scalable_chat
      POSTGRES_USER: chat_user
      POSTGRES_PASSWORD: chat_password
    ports:
      - "5432:5432"

  redis:
    image: redis:7
    container_name: scalable-chat-redis
    ports:
      - "6379:6379"
